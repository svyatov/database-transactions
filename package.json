{
  "name": "database-transactions",
  "version": "0.1.0",
  "description": "Learn database transactions from verified, runnable examples — every claim proven against a real database",
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3"
  },
  "repository": "github:svyatov/database-transactions",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "gen": "bun scripts/gen-transcripts.ts",
    "lesson": "bun scripts/lesson.ts",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/bun": "^1.3.14",
    "vitepress": "^1.6.4"
  }
}
